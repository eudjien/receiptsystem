pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

def coreProjectName = "checksystem-core"
include coreProjectName
project(":$coreProjectName").projectDir = file("../$coreProjectName")

rootProject.name = 'checksystem-webapi'
