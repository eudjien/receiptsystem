pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'checksystem'

include 'checksystem-core'
include 'checksystem-cli'
include 'checksystem-webuiservlet'
include 'checksystem-webmvc-jdbc'
include 'checksystem-webapi'
