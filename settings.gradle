pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'checksystem'
include('checksystem-core', 'checksystem-cli', /* 'checksystem-web' */)

